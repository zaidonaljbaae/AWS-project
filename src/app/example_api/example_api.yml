handler: app/example_api/example_api.handler
timeout: 15
memorySize: 256

package:
  patterns:
    - "app/example_api/**"

events:
  - httpApi:
      path: /api/{proxy+}
      method: ANY

# Keep permissions minimal. Add per-service permissions as you need.
# iamRoleStatements: []
